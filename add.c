#include "monty.h"

/**
 * add - adds a new node at the end of a stack_t list.
 *
 * @stack: current node.
 * @line_number: the new node's data.
 *
 */
void add(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
